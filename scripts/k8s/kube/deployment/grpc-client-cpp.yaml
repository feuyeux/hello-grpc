apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: grpc-best
  name: grpc-client-cpp
  labels:
    app: grpc-client-cpp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grpc-client-cpp
  template:
    metadata:
      labels:
        app: grpc-client-cpp
    spec:
      serviceAccountName: grpc-best-sa
      containers:
        - name: grpc-client-cpp
          image: feuyeux/grpc_client_cpp:1.0.0
          command: ["/bin/sleep", "3650d"]
          env:
            - name: GRPC_SERVER
              value: "grpc-server-svc.grpc-best.svc.cluster.local"
          imagePullPolicy: Always
