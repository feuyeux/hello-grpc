apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: grpc-best
  name: grpc-client-php
  labels:
    app: grpc-client-php
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grpc-client-php
  template:
    metadata:
      labels:
        app: grpc-client-php
    spec:
      serviceAccountName: grpc-best-sa
      containers:
        - name: grpc-client-php
          image: feuyeux/grpc_client_php:1.0.0
          command: ["/bin/sleep", "3650d"]
          env:
            - name: GRPC_SERVER
              value: "grpc-server-svc.grpc-best.svc.cluster.local"
          imagePullPolicy: Always
