apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: grpc-best
  name: grpc-client-dart
  labels:
    app: grpc-client-dart
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grpc-client-dart
  template:
    metadata:
      labels:
        app: grpc-client-dart
    spec:
      serviceAccountName: grpc-best-sa
      containers:
        - name: grpc-client-dart
          image: feuyeux/grpc_client_dart:1.0.0
          command: ["/bin/sleep", "3650d"]
          env:
            - name: GRPC_SERVER
              value: "grpc-server-svc.grpc-best.svc.cluster.local"
          imagePullPolicy: Always
