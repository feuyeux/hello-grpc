apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: grpc-best
  name: grpc-client-swift
  labels:
    app: grpc-client-swift
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grpc-client-swift
  template:
    metadata:
      labels:
        app: grpc-client-swift
    spec:
      serviceAccountName: grpc-best-sa
      containers:
        - name: grpc-client-swift
          image: feuyeux/grpc_client_swift:1.0.0
          command: ["/bin/sleep", "3650d"]
          env:
            - name: GRPC_SERVER
              value: "grpc-server-svc.grpc-best.svc.cluster.local"
          imagePullPolicy: Always
