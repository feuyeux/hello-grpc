apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: grpc-best
  name: grpc-client-ts
  labels:
    app: grpc-client-ts
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grpc-client-ts
  template:
    metadata:
      labels:
        app: grpc-client-ts
    spec:
      serviceAccountName: grpc-best-sa
      containers:
        - name: grpc-client-ts
          image: feuyeux/grpc_client_ts:1.0.0
          command: ["/bin/sleep", "3650d"]
          env:
            - name: GRPC_SERVER
              value: "grpc-server-svc.grpc-best.svc.cluster.local"
          imagePullPolicy: Always
