<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="http://maven.apache.org/POM/4.0.0"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>org.feuyeux.grpc</groupId>
    <artifactId>hello-grpc-java</artifactId>
    <version>1.0.0-SNAPSHOT</version>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <jdk.version>25</jdk.version>
        <!-- grpc-java 1.82.1 is the current stable Maven release at the
             time of writing; +0.4 minor over the prior 1.78.0 baseline.
             Upstream has not shipped 1.83.x+ yet, so we hold this pin.
             The deps that this project pulls in (grpc-netty, grpc-services,
             javax.annotation) all support 1.82.1 without changes. -->
        <grpc.version>1.82.1</grpc.version>
        <protoc.version>3.24.3</protoc.version>
        <!-- netty-resolver-dns 4.2.15.Final is current upstream, but
             grpc-netty 1.82.1 is compiled against the 4.2.9 API; bumping
             the native classifier here without bumping grpc-netty can
             produce ABI warnings on macOS. Keep aligned with grpc-java. -->
        <netty-resolver-dns.version>4.2.9.Final</netty-resolver-dns.version>
        <guava.version>33.6.0-jre</guava.version>
        <jetcd.version>0.8.6</jetcd.version>
        <protobuf-maven-plugin.version>0.6.1</protobuf-maven-plugin.version>
        <os-maven-plugin.version>1.7.1</os-maven-plugin.version>
        <annotations-api.version>6.0.53</annotations-api.version>
        <maven-shade-plugin.version>3.6.2</maven-shade-plugin.version>
        <!-- JUnit 5.14.4 is the current 5.x stable. We deliberately
             stay on the 5.x line because the 6.x line requires JUnit
             Platform 6 + JDK 17+ and changes test annotation handling;
             the project's tests use jupiter 5 APIs that are stable
             across 5.13 -> 5.14. -->
        <junit.version>5.14.4</junit.version>
        <system-rules.version>1.19.0</system-rules.version>
        <!-- maven-compiler-plugin 4.0.0 is still in beta upstream;
             keep the last 3.x stable. -->
        <maven-compiler-plugin.version>3.15.0</maven-compiler-plugin.version>
        <!-- log4j 3.0.0 is in beta; stay on the 2.x stable line. -->
        <log4j-slf4j-impl.version>2.26.1</log4j-slf4j-impl.version>
        <nacos.version>3.2.2</nacos.version>
        <lombok.version>1.18.46</lombok.version>
        <fmt-maven-plugin.version>2.29</fmt-maven-plugin.version>
        <exec-maven-plugin.version>3.6.2</exec-maven-plugin.version>
        <!-- OpenTelemetry + contrib gRPC instrumentations.
             We use the opentelemetry-java-contrib library
             (io.opentelemetry.instrumentation:opentelemetry-grpc-1.6)
             instead of io.grpc:grpc-opentelemetry because the latter
             does not provide a stable public io.grpc.ServerInterceptor /
             ClientInterceptor API for grpc-java 1.82.1. The contrib
             library does, via TracingServerInterceptor /
             TracingClientInterceptor. Both are wired from env var
             GRPC_HELLO_OTEL=Y in OtelSupport.java.
             2.29.0-alpha is the current alpha line; contrib tracks
             otel-core major.minor. -->
        <opentelemetry.version>1.63.0</opentelemetry.version>
        <opentelemetry.contrib.version>2.29.0-alpha</opentelemetry.contrib.version>
    </properties>

    <!-- grpc-netty 1.82.1 transitively pins io.netty:* to 4.1.133.Final.
         Netty 4.1.x still calls sun.misc.Unsafe.allocateMemory, which the
         JDK 25 JVM prints a "terminally deprecated" warning for on every
         allocation. Netty 4.2.x replaces those Unsafe calls with the
         JDK-internal MemorySegment/VarHandle APIs and is binary-compatible
         with the 4.1.x public surface that grpc-netty uses. We force the
         entire io.netty family to 4.2.9.Final here (matching the
         netty-resolver-dns pin) so the project ships a single netty
         version. -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-bom</artifactId>
                <version>4.2.9.Final</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- grpc-protobuf 1.82.1 transitively pins protobuf-java to
                 3.25.8. The 3.25.x UnsafeUtil still calls
                 sun.misc.Unsafe.arrayBaseOffset, which JDK 25 prints a
                 removal warning for. protobuf-java 4.35.1 dropped those
                 Unsafe.arrayBaseOffset / arrayIndexScale calls and made
                 sun.misc an optional OSGi import. We force 4.35.1 here.
                 Compatibility note: grpc-protobuf 1.82.1 only touches
                 Message / MessageLite / ByteString public APIs, which
                 are unchanged between 3.x and 4.x. The codegen output
                 from protobuf-maven-plugin 0.6.1 (proto3 descriptors)
                 is forward-compatible with protobuf-java 4.x runtime. -->
            <!-- grpc-protobuf 1.82.1 transitively pins protobuf-java to
                 3.25.8. protobuf-java 3.25.x's UnsafeUtil still calls
                 sun.misc.Unsafe.arrayBaseOffset, which JDK 25 prints a
                 removal warning for. protobuf-java 4.x dropped those
                 calls, but the 3.x to 4.x jump breaks grpc-java 1.82.1
                 at runtime: InProcessServerBuilder implements an
                 internal ServerImplBuilder$ClientTransportServersBuilder
                 interface that the 3.x protobuf-java compile wired up
                 against certain generic signatures; 4.x changed those
                 signatures and we get AbstractMethodError on test
                 setup. We must stay on protobuf-java 3.25.x until we
                 can also bump grpc-java to a 1.83+ release that is
                 compiled against 4.x. The Unsafe warning is therefore
                 accepted on this branch and noted in the comment
                 above as a known JDK 25 noise source. -->
            <!-- (3.25.8 is pinned transitively by grpc-protobuf 1.82.1;
                 do NOT override to 4.x here.) -->
            <!--
            <dependency>
                <groupId>com.google.protobuf</groupId>
                <artifactId>protobuf-java</artifactId>
                <version>4.35.1</version>
            </dependency>
            <dependency>
                <groupId>com.google.protobuf</groupId>
                <artifactId>protobuf-java-util</artifactId>
                <version>4.35.1</version>
            </dependency>
            -->
            <!-- nacos-api 3.2.2 transitively pulls grpc-inprocess 1.78.0
                 and grpc-netty-shaded 1.78.0. The older grpc-inprocess
                 InProcessServerBuilder does not implement the
                 ServerImplBuilder$ClientTransportServersBuilder.build
                 ClientTransportServers(List, MetricRecorder) method that
                 grpc-core 1.82.1 calls, producing AbstractMethodError
                 at LandingServiceImplTest.setUp. Force the entire
                 io.grpc:* family to 1.82.1 so the InProcessServerBuilder
                 we resolve at test time is the 1.82.1 one that matches
                 grpc-core's interface. -->
            <dependency>
                <groupId>io.grpc</groupId>
                <artifactId>grpc-inprocess</artifactId>
                <version>${grpc.version}</version>
            </dependency>
            <dependency>
                <groupId>io.grpc</groupId>
                <artifactId>grpc-netty-shaded</artifactId>
                <version>${grpc.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>
        <!-- OpenTelemetry SDK + a logging exporter so spans show up
             without needing a sidecar Jaeger / OTLP collector. Swap
             in OtlpGrpcSpanExporter in OtelSupport.java for prod. -->
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-api</artifactId>
            <version>${opentelemetry.version}</version>
        </dependency>
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-sdk</artifactId>
            <version>${opentelemetry.version}</version>
        </dependency>
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-exporter-logging</artifactId>
            <version>${opentelemetry.version}</version>
        </dependency>
        <!-- Semantic conventions for service attributes -->
        <dependency>
            <groupId>io.opentelemetry.semconv</groupId>
            <artifactId>opentelemetry-semconv</artifactId>
            <version>1.42.0</version>
        </dependency>
        <!-- Public gRPC ServerInterceptor / ClientInterceptor for
             grpc-java 1.82 (provided by opentelemetry-java-contrib
             library; see OtelSupport.java for usage). -->
        <dependency>
            <groupId>io.opentelemetry.instrumentation</groupId>
            <artifactId>opentelemetry-grpc-1.6</artifactId>
            <version>${opentelemetry.contrib.version}</version>
        </dependency>
        <dependency>
            <groupId>io.grpc</groupId>
            <artifactId>grpc-netty</artifactId>
            <version>${grpc.version}</version>
            <exclusions>
                <exclusion>
                    <artifactId>error_prone_annotations</artifactId>
                    <groupId>com.google.errorprone</groupId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>io.grpc</groupId>
            <artifactId>grpc-protobuf</artifactId>
            <version>${grpc.version}</version>
            <exclusions>
                <exclusion>
                    <artifactId>proto-google-common-protos</artifactId>
                    <groupId>com.google.api.grpc</groupId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>io.grpc</groupId>
            <artifactId>grpc-stub</artifactId>
            <version>${grpc.version}</version>
        </dependency>
        <dependency>
            <groupId>io.grpc</groupId>
            <artifactId>grpc-services</artifactId>
            <version>${grpc.version}</version>
            <exclusions>
                <exclusion>
                    <artifactId>error_prone_annotations</artifactId>
                    <groupId>com.google.errorprone</groupId>
                </exclusion>
                <exclusion>
                    <artifactId>gson</artifactId>
                    <groupId>com.google.code.gson</groupId>
                </exclusion>
                <exclusion>
                    <artifactId>guava</artifactId>
                    <groupId>com.google.guava</groupId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>io.etcd</groupId>
            <artifactId>jetcd-core</artifactId>
            <version>${jetcd.version}</version>
            <exclusions>
                <exclusion>
                    <artifactId>slf4j-api</artifactId>
                    <groupId>org.slf4j</groupId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
            <version>${guava.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-slf4j-impl</artifactId>
            <version>${log4j-slf4j-impl.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.tomcat</groupId>
            <artifactId>annotations-api</artifactId>
            <version>${annotations-api.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-resolver-dns-native-macos</artifactId>
            <version>${netty-resolver-dns.version}</version>
            <classifier>osx-x86_64</classifier>
        </dependency>
        <dependency>
            <groupId>com.alibaba.nacos</groupId>
            <artifactId>nacos-client</artifactId>
            <version>${nacos.version}</version>
            <classifier>pure</classifier>
        </dependency>
        <dependency>
            <groupId>com.alibaba.nacos</groupId>
            <artifactId>nacos-common</artifactId>
            <version>${nacos.version}</version>
        </dependency>
        <dependency>
            <groupId>com.alibaba.nacos</groupId>
            <artifactId>nacos-api</artifactId>
            <version>${nacos.version}</version>
            <exclusions>
                <exclusion>
                    <artifactId>proto-google-common-protos</artifactId>
                    <groupId>com.google.api.grpc</groupId>
                </exclusion>
                <exclusion>
                    <artifactId>protobuf-java</artifactId>
                    <groupId>com.google.protobuf</groupId>
                </exclusion>
                <exclusion>
                    <artifactId>grpc-api</artifactId>
                    <groupId>io.grpc</groupId>
                </exclusion>
                <exclusion>
                    <artifactId>grpc-protobuf</artifactId>
                    <groupId>io.grpc</groupId>
                </exclusion>
                <exclusion>
                    <artifactId>grpc-core</artifactId>
                    <groupId>io.grpc</groupId>
                </exclusion>
                <exclusion>
                    <artifactId>grpc-stub</artifactId>
                    <groupId>io.grpc</groupId>
                </exclusion>
                <exclusion>
                    <artifactId>grpc-util</artifactId>
                    <groupId>io.grpc</groupId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.github.stefanbirkner</groupId>
            <artifactId>system-rules</artifactId>
            <version>${system-rules.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.grpc</groupId>
            <artifactId>grpc-testing</artifactId>
            <version>${grpc.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <extensions>
            <extension>
                <groupId>kr.motd.maven</groupId>
                <artifactId>os-maven-plugin</artifactId>
                <version>${os-maven-plugin.version}</version>
            </extension>
        </extensions>
        <finalName>${project.name}</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-compiler-plugin.version}</version>
                <configuration>
                    <encoding>UTF-8</encoding>
                    <source>${jdk.version}</source>
                    <target>${jdk.version}</target>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                    <!-- Run javac in a forked JVM so we can pass
                         sun.misc.Unsafe memory access flags to the
                         JVM that actually executes lombok.permit.Permit's
                         static initializer. Without forking, javac runs
                         in-process under Maven and any J arg is ignored.
                         The fork also isolates javac's stderr from
                         Maven's stdout so removal warnings from Permit's
                         static initializer block don't pollute the
                         build log. -->
                    <fork>true</fork>
                    <compilerArgs>
                        <arg>--add-exports</arg>
                        <arg>jdk.compiler/com.sun.tools.javac.parser=ALL-UNNAMED</arg>
                        <!-- Disable the [removal] lint category so javac
                             itself does not surface the sun.misc.Unsafe
                             removal warning on every compile pass. -->
                        <arg>-Xlint:-removal</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.xolstice.maven.plugins</groupId>
                <artifactId>protobuf-maven-plugin</artifactId>
                <version>${protobuf-maven-plugin.version}</version>
                <configuration>
                    <clearOutputDirectory>false</clearOutputDirectory>
                    <pluginArtifact>io.grpc:protoc-gen-grpc-java:${grpc.version}:exe:${os.detected.classifier}</pluginArtifact>
                    <pluginId>grpc-java</pluginId>
                    <protoSourceRoot>${project.basedir}/src/main/proto</protoSourceRoot>
                    <protocArtifact>com.google.protobuf:protoc:${protoc.version}:exe:${os.detected.classifier}</protocArtifact>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>compile</goal>
                            <goal>compile-custom</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>com.spotify.fmt</groupId>
                <artifactId>fmt-maven-plugin</artifactId>
                <version>${fmt-maven-plugin.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>format</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>${exec-maven-plugin.version}</version>
                <configuration>
                    <cleanupDaemonThreads>false</cleanupDaemonThreads>
                    <daemonThreadJoinTimeout>0</daemonThreadJoinTimeout>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>server</id>
            <build>
                <finalName>hello-grpc-java-server</finalName>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-shade-plugin</artifactId>
                        <version>${maven-shade-plugin.version}</version>
                        <configuration>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                        </configuration>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>shade</goal>
                                </goals>
                                <configuration>
                                    <transformers>
                                        <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer" />
                                        <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                            <mainClass>org.feuyeux.grpc.server.ProtoServer</mainClass>
                                            <manifestEntries>
                                                <Multi-Release>true</Multi-Release>
                                            </manifestEntries>
                                        </transformer>
                                    </transformers>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>client</id>
            <build>
                <finalName>hello-grpc-java-client</finalName>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-shade-plugin</artifactId>
                        <version>${maven-shade-plugin.version}</version>
                        <configuration>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                        </configuration>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>shade</goal>
                                </goals>
                                <configuration>
                                    <transformers>
                                        <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer" />
                                        <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                            <mainClass>org.feuyeux.grpc.client.ProtoClient</mainClass>
                                            <manifestEntries>
                                                <Multi-Release>true</Multi-Release>
                                            </manifestEntries>
                                        </transformer>
                                    </transformers>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>