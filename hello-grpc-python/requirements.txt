# python -m grpc.tools.protoc --version
# 由 grpcio-tools 版本决定； 注意与 protoc --version兼容
# https://pypi.org/project/grpcio-tools/
grpcio-tools==1.81.1
# https://pypi.org/project/protobuf/
# grpcio-tools 1.71.0 depends on protobuf<6.0dev and >=5.26.1
protobuf==6.33.5
# OpenTelemetry SDK + stdout exporter. Pinned to the same minor
# versions as opentelemetry-instrumentation-grpc to avoid the
# "Allerlei" / "Alligator" distro split. No-op when GRPC_HELLO_OTEL
# is not "Y" so this is purely additional install cost, no runtime
# behavior change in the default case.
# opentelemetry-proto (pulled in by opentelemetry-exporter-otlp-proto-common)
# requires protobuf<7.0,>=5.0 as of 1.38.0, which is compatible with the
# protobuf==6.33.5 pinned above (older 1.27.0 line required protobuf<5.0
# and conflicted with grpcio-tools' protobuf>=6.33.5 requirement).
opentelemetry-api==1.38.0
opentelemetry-sdk==1.38.0
opentelemetry-exporter-otlp-proto-common==1.38.0
# https://pypi.org/project/opentelemetry-instrumentation-grpc/
opentelemetry-instrumentation-grpc==0.59b0
# B7 — gRPC Health Check (bundled with grpcio; pinned to same version)
# https://pypi.org/project/grpcio-health-checking/
grpcio-health-checking==1.82.0
# C4 — gRPC Server Reflection
# https://pypi.org/project/grpcio-reflection/
grpcio-reflection==1.81.1